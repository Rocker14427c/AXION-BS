<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Spsm" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@color/black</item>
        <item name="android:colorBackground">@color/black</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/black</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="android:colorAccent">@color/accent</item>
        <item name="android:colorPrimary">@color/accent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:navigationBarDividerColor">@color/black</item>
    </style>

    <style name="Theme.Spsm.Home" parent="Theme.Spsm">
        <item name="android:windowShowWallpaper">false</item>
        <!--
            The full-screen window item was set here and it hid the status bar:
            the bar goes away, a swipe from the top brings it back for a second
            or two, and then it hides again - which is exactly what the phone
            kept doing. The bar is part of the mode's own interface (clock,
            battery, the way back), so it stays. Nothing here may ask for it to
            be hidden.
        -->
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/black</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
    </style>
</resources>
