# LIVE WORKLOG - 2026-09-26 session (pushed every few minutes, per owner's rule)

Purpose: the last session was lost to sandbox failures mid-work; the owner ordered a
checkpoint doc on GitHub updated ~every 5 minutes so nothing dies silently again.
Newest entries at the TOP. Times are device-local (IST) unless marked.

## 2026-09-26 ~10:05 IST - reconciliation committed: v3.10.1 tree = device v3.9.2 + both field bugs fixed

- **a87a221 (pushed)**: adopted the device-verified v3.9.2 scripts (three-way diff proved
  v3.9.1 == GitHub HEAD for every script, and v3.9.2 a strict superset for lib.sh - nothing
  was lost by adopting wholesale) + the device binaries (tool jar 12a80b49, any-slot
  gesturemon) + module.prop v3.10.1/79.
- **Fix 1 - false drift (the 70s exit)**: root cause was NOT string comparison. The kernel
  refused cpu6/online in BOTH directions (sleep write never took - the applied journal says
  1, honestly - and the identical write-back got EPERM). The restore counted the refused
  no-op as "failed ... want [1] got [1]" -> 1 drifted knob -> safety valves -> 70s. Now a
  refused write whose value ALREADY stands at the recorded original is logged "wrote".
  An unreadable value still fails (guard compares a real reading). Regression test 7b:
  chmod 444 on the rig's cpu6/online reproduces the exact device scenario end to end.
- **Fix 2 - gesture dispatch (recognised swipes went nowhere)**: gesturemon's defaults were
  the native cmd binary, whose binder dies in the recognizer's nohup'd root context
  ("Failure calling service input: Failed transaction" - 3 recognitions, 0 dispatches in
  bench4). Defaults now the app_process wrappers (input keyevent 3 / am start ...), which
  the engine itself uses from the same context class all day. cfg still overrides; section
  94 now asserts the argv the recognizer is started with.
- **Fix 3 - any-slot recognizer in SOURCE**: HEAD's gesturemon.c still had the slot-0-only
  filter (the panel hands real swipes slots 2/3/5/7). Rewrote the MT state machine: first
  finger DOWN owns the gesture on any slot, other fingers are noise until the owner lifts.
  Zig 0.13 downloaded, both ABIs rebuilt (arm64 58,360B). Device binaries kept in recovery/.
- **Jar source parity recovered**: jadx-decompiled the device jar's classes.dex - the lost
  session's Main.java changes were: ArrayBlockingQueue(1) (a SynchronousQueue offer with no
  waiter DROPPED the rc arriving during readAll -> every op cost the full 30s ceiling),
  pumpBinder() = a cheap getService("activity") between <=250ms poll slices (an idle
  app_process JVM does not get binder callbacks serviced promptly), one nanoTime deadline.
  Ported all three into tool/src/Main.java with the evidence in comments, rebuilt via
  ecj+d8: strings now DIFF-CLEAN against the device jar (same D8 header sha, only binary
  gobblygook differs). module/bin jar = the rebuild (source<->binary parity); the
  field-proven device jar stays in recovery/ as fallback.
- Suites: run 1 on the adopted tree = **742/0 + 252/0 + 19/0 + 22/0 all green**. A second
  run showed 83 failures - all timing-bounded waits, cause = my own toolchain builds
  (pip/jadx/ecj/d8) hammering the sandbox CPU concurrently; re-running clean now (run 3,
  early sections green). Next after suites: deploy v3.10.1 to the phone, bench5 (activate/
  exit/deep-grace/exit-race), synthetic gesture proof with the FIXED dispatch (sendevent
  harness from bench4, corrected recents injection: <=40px movement, 400ms hold), stranding
  scan, then release + owner's finger test.

## 2026-09-26 ~09:25 IST - forensics complete, the lost session's state fully mapped

- **The phone runs v3.9.2/78** - the lost session's own version line, never committed. Its
  scripts are the repo's perf round (deep grace, native cmd paths, spsm-tool, gesturemon)
  PLUS post-commit fixes, all recovered into `recovery/device-2026-09-25/` and pushed (c3b72f6):
  1. **spsm-tool rc=134 root cause + fix**: KernelSU keeps /data/adb at 700 root, so uid 2000
     cannot read the jar and ART aborts with a misleading DEX2OAT message. Fix: runtime copy at
     /data/local/tmp/spsm/spsm-tool.jar, proof-of-life runs from THAT copy. Device jar
     (6,256 B, md5 12a80b49) differs from the committed one (5,856 B, f2ef2c46) - strings diff
     in progress; repair.log proves the device jar works (171 pkgs read, 111 writes, 0 failures).
  2. **The 21:27 stranding contained**: journal-first ordering (records land BEFORE writes,
     batch and fan), "applying" journal state so the exit sweep never deletes an in-flight
     knob's records, bounded exit wait on deep_restrict.pid, workers stand down when $ACTIVE
     dies, failed reads restore from record instead of being skipped. repair.log: the 31
     buckets + 80 appops stranded that night were repaired (writes=111, rc=0).
  3. **bench4.log (23:48) - the lost session's last benchmark**: deep grace fired at +74s
     exactly as designed; the exit race was caught live (deactivate 8s into a restrict pass ->
     bounded wait -> clean revert); gesturemon RECOGNISED every synthetic swipe
     (home dy=209/177ms, dy=281/210ms, dy=183/129ms) but **every dispatch failed**:
     "cmd: Failure calling service input: Failed transaction".
- **Bug A (gestures)**: the dispatch defaults are `cmd input keyevent 3` / `cmd activity start`
  - the native `cmd` binder fails from gesturemon's nohup'd root context, while the engine's
  app_process `input`/`am` calls work from the same context all day. start_gesturemon passes
  commands explicitly, so the fix is script-side defaults (no binary rebuild; zig is gone from
  the sandbox anyway). C defaults get patched for future builds.
- **Bug B (false drift)**: "WARN cores_sleep did not return: want [1] got [1] " - trailing
  whitespace in the compared reading; one exit cost 70s over it (safety valves + re-verify).
  Fix: trim in the drift comparison. Also silence safety_force writing cpu0/online (EPERM noise;
  cpu0 is not offlineable on this kernel).
- Plan next: adopt the device scripts as the tree of record, apply fixes A+B, suite, redeploy
  as v3.10.1/79, re-benchmark (activate/exit/deep/grace), synthetic-swipe gesture proof with
  working dispatch, then the owner's finger. Release + notes after.

## 2026-09-26 ~08:50 IST - session start, situation assessment

- Sandbox was rewound overnight: `.git/config`, `sshpass`, `+x` bits all had to be rebuilt.
  Recovered the branch from GitHub (`git reset --hard origin/Rewrite` -> `3ed7f10`).
- GitHub state: Rewrite @ `97669b2` after I published v3.10.0 (bump + notes + zip + release)
  earlier today. Releases: v3.9.0 (Sep 25) and v3.10.0 (Sep 26). **No v3.9.1/v3.9.2 tag or
  release exists on GitHub.**
- Device probe (tunnel revived by owner, endpoint libiy-...:42199):
  - `/data/adb/spsm/native/` holds spsm-gesturemon (58,200 B), spsm-screenmon (29,592 B),
    spsm-tool.jar (6,256 B), all dated **Sep 25 23:46** - sizes DIFFER from the repo's
    committed binaries (58,104 / - / 5,856): the lost session rebuilt and deployed past its
    last commit. DEVICE IS THE ONLY COPY - recovering it into the repo this hour.
  - `spsm.log` tail shows a 23:53 exit on NEWER code than the repo: `WARN cores_sleep did not
    return: ... want [1] got [1]` (a comparison bug - warns when values MATCH), then
    `exit: 1 value(s) could not be restored - forcing the safety valves`,
    `safety_force: cores online, governor schedutil`, `revert finished in 18s with 1 drifted
    knob(s)`. The drift was false (want==got) - to fix after recovery.
  - Termux reinstalled (F-Droid -> Play): **new uid 10253**; deviceidle AND netpolicy
    whitelists already carry 10253 (fixed by the lost session or the owner). Battery 49%,
    charging.
- Plan: (1) pull every device-only artifact (scripts, binaries, module.prop, config, logs,
  /data/local/tmp benchmark material) into the repo and push; (2) diff device vs HEAD, adopt
  the newer work as the tree of record; (3) fix the cores_sleep WARN false-drift; (4) suites;
  (5) on-phone benchmark round + gesture proof (input-swipe driven, then owner's finger);
  (6) release whatever wins. Worklog pushed at every step.
