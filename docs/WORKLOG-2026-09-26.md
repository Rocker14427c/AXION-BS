# LIVE WORKLOG - 2026-09-26 session (pushed every few minutes, per owner's rule)

Purpose: the last session was lost to sandbox failures mid-work; the owner ordered a
checkpoint doc on GitHub updated ~every 5 minutes so nothing dies silently again.
Newest entries at the TOP. Times are device-local (IST) unless marked.

## 11:20 IST - recents SecurityException FIXED (APK v3.10.1 installed) + injector protocol postmortem

**The last dispatch blocker is gone.** Full exception captured: `SecurityException:
Permission Denial: starting Intent ... SpsmRecentsActivity from uid=2000 not
exported from uid 10225` - the repo's own manifest declared the recents activity
`exported="false"`, so EVERY recognised hold-swipe (the owner's dy=352 and
dy=170 included) dispatched into a permission wall. The installed companion
was still 3.8.3; the lost session's AxionSPSM-395.apk (same md5 as the
module-bundled app) was never installed and carried the same flag.

Fix, built and deployed:
- manifest: SpsmRecentsActivity exported=true (comment explains: launched by
  the mode's OWN dispatcher under shell identity; excludeFromRecents+singleTop
  unchanged; the screen shows the mode's recents and nothing else)
- APK rebuilt with the repo toolchain (re-extracted JDK - the restore ate
  lib/ again - plus setup-toolchain: aapt2/d8/ecj/apksigner/minapk debug
  keystore = same signing key as the installed app) -> v3.10.1 (79)
- phone: `pm install -r` = Success, in-place upgrade, versionName=3.10.1
- device proof: `su 2000 -c "am start ... SpsmRecentsActivity"` -> Starting:,
  NO exception, mCurrentFocus = dev.axion.spsm/.SpsmRecentsActivity
- commit 7133586 pushed; earlier 6d0838c restored +x modes (module scripts
  had them wiped in worktree+index by the sandbox restores)

**Synthetic-injector postmortem (important, corrects the record):** this
toybox `sendevent` takes ONE event per call ("Max 4 arguments" on batch
syntax) - the earlier inject_home.sh batch form NEVER emitted anything.
Every "fire" logged during its runs (dy=207, dy=259, dy=352, dy=170) was
the owner's real finger - timestamps match the 10:45 finger test exactly.
The corrected per-call injector still produced no fires, and the reason is
now known: it used code 58 for slot selection, but **ABS_MT_SLOT is 47
(0x2f); 58 is ABS_MT_PRESSURE** - it never selected a slot and sprayed
pressure=-1 garbage. Injector codes fixed in /home/user/proofInject.sh for
the next hands-off window. Meanwhile the owner is actively using the phone
(Via focus at 11:14), so synthetic runs are invalid right now anyway.

Also solved: the "focus moved without a gm fire line" mystery from the
earlier home test - SpsmHomeActivity is the registered HOME (dumpsys shows
the HOME filter), so the SYSTEM gesture pipeline routes swipe-up to it even
with the launcher blocked. gm remains the reliable path for BOTH gestures
(system recents dies with the launcher; gm dispatches the activity itself).

Suites: run5's 7 failures were collateral of a mid-run sandbox restore
(/tmp/spsm-test wiped; sections 13-16 only; later sections re-created the
rig and passed) - NOT the su-2000 change. Final gate (run6) now running on
the exported-recents tree; codec+daemon+install suites follow.

Next: owner finger test (fast flick = home, slow drag+hold = recents) with
the fixed dispatch chain live, then release v3.10.1.

## 2026-09-26 ~13:10 IST - dispatch root cause #2: the UID. Owner finger test = recognised but not dispatched

- The stdin fix (eec2249) deployed byte-verified (/proc/PID/fd/0 -> /dev/null confirmed on
  the phone) and it DID remove one failure mode - but the owner's finger test at ~10:45
  showed the remaining one, and gesturemon.log told the whole story:
  `recents (dy=352 held 350ms)` + `recents (dy=170 held 350ms)` - **the owner's real
  swipes were RECOGNISED by the rebuilt any-slot binary** (real-finger recognition: PROVEN)
  - each followed by `cmd: Failure calling service activity: Failed transaction`. On this
  ROM both `input` and `am` are cmd-fronted, and cmd hands its fds AND its caller identity
  to the service. gesturemon runs as root; every service call this module has ever proven
  on this phone runs under **su 2000** (the shell identity the permission checks know).
  Two days, two services, 100% failure from the root context; thousands of successes from
  the su-2000 context. That is the evidence.
- **Fix staged**: dispatch defaults now `su 2000 -c 'input keyevent 3'` and
  `su 2000 -c 'am start ... SpsmRecentsActivity'` (lib.sh + C source + section 94 argv
  assertions updated); both ABIs rebuilt (arm64 58,672B, md5 8a1a5324). Suites running on
  the change now (quiet box this time).
- Synthetic-injection note: two clean-looking synthetic runs did not fire while focus moved
  Termux->Via on its own - the owner's fingers were on the phone (the any-slot logic
  correctly treats a second finger as noise). The manual-test protocol from now on: phone
  untouched, screen awake, one injection, log+focus immediately after.
- bench5 round 2 stands as the device proof for everything else: exit race perfect
  (grace +69s, deactivate 8s into the pass, "revert clean in 32s", no WARN, no safety
  valves, states_left=0, stranding scan clean), clean cycle 34s/22s "revert clean in 19s",
  tool+native republished and kernel-proven. radio_state.tsv persistence = by design.
- Tunnel flapped again mid-verification (nlduz session died; gist lagging). Awaiting the
  owner's restart to: run the root-vs-2000 dispatch matrix (A-F) as the final confirmation,
  deploy the su-2000 defaults, one clean synthetic proof, and the owner's finger round 2.

## 2026-09-26 ~12:30 IST - first bench5 exposed a STALE DEPLOY; phone now byte-verified; bench5 re-running

- v3.10.1 deployed at 10:17 IST and self-proven (publish_native through the new lib:
  "batch tool: published" rc=2 via su 2000, "native helpers: arm64-v8a" - the zig-rebuilt
  gesturemon 58,360B and screenmon 26,800B load and run on the kernel; module.prop 79).
- **But bench5 round 1 showed both bugs STILL firing** - WARN want[1] got[1] at 10:20/10:21,
  and gesturemon (fresh pid, started by the new-to-me lib) dispatching via cmd with 9
  "Failure calling service input" lines. Forensics: md5sum of the phone's scripts vs the
  repo's - **the first payload tar was stale** (built at the exact moment one of the
  sandbox's flaky restores had the worktree rolled back; the phone got v3.9.2 scripts with
  a v3.10.1 module.prop). One stale deploy explains BOTH residuals: old knobs.sh had no
  no-op guard, old lib.sh still defaulted to cmd.
- Recovery: local git was also rolled back (parallel history, add/add rebase conflicts on
  my own files) - aborted, reset --hard to origin/Rewrite (which provably holds the patched
  tree, a87a221+5ecf43b), verified every patch byte in the worktree, rebuilt the payload,
  re-shipped, re-installed - and this time verified the chain end to end: all 9 script md5s
  on the phone == repo (lib 8279476..., knobs 3beabfe...), natives republished 10:26.
- Round-1 numbers that ARE valid (they measured v3.9.2 scripts + new natives + new jar,
  i.e. exactly what the phone ran): activate 34s, raced deactivate 31s (bounded stand-down
  worked: pidfile gone, stranding scan clean - whatsapp/gm/vivimusic/nextplayer/chatgpt
  bucket=10 op=default; instagram op=ignore is its restored record value; edge.bar/doze
  bucket=5 are ROM defaults), clean cycle 46s/25s, deep grace fired at +73s. journal_states_
  left=1 + tsv_left=1 = the cores_sleep slice left in restored-drift by the false WARN -
  round 2 must show 0.
- Round-1 gesture section was contaminated: focus_before was already SpsmHomeActivity
  (home_swap puts SPSM's own home in front), fires=9 with human-scale dy (212-243px,
  146-210ms) = real fingers on the phone during the synthetic test. Round 2 interpretation:
  home dispatch = focus stays/returns SpsmHomeActivity + zero "Failure" lines; recents =
  SpsmRecentsActivity focus.
- Also fixed en route: sandbox restore wiped sshpass (sudo apt reinstall), +x bits (again),
  git identity/origin (again), and poisoned the edge cache with an IPv6 address (flushed;
  Pinggy routed by port on the IPv4 edges while the xzvdr session's AAAA-only DNS flickered).
- bench5 round 2 launching now against the byte-verified tree. Release v3.10.1 is cut when
  its log is green: dispatch_failures=0, focus moves, no WARN, states_left=0.

## 2026-09-26 ~11:00 IST - suites green on the reconciled tree; release notes drafted; waiting on tunnel

- Clean re-run finished: **749/0 main** (incl. new 7b refused-no-op-write regression + the
  section-94 dispatch-argv assertions), **252/0 codec, 19/0 daemon, 22/0 install**. The 83
  earlier failures were confirmed as sandbox CPU contention from my own concurrent toolchain
  builds - every one was a bounded-wait assertion, and they all pass on a quiet box.
- docs/RELEASE-v3.10.1.md drafted and committed: version map (GitHub v3.10.0/77 vs device
  v3.9.2/78 vs this v3.10.1/79), what v3.9.2 brought, both fixes with the device evidence,
  the recovered Main.java pump, suite numbers, and an honest device-verification ledger -
  the release is CUT only when bench5 comes back green from the phone; until then v3.10.0
  stays the latest device-benched zip.
- Deploy payload /tmp/deploy310.tgz ready (scripts + module.prop + rebuilt natives + jar +
  bench5.sh). Tunnel checked again at the end of this entry - still expired (edge closes
  the connection). Waiting on the owner's Termux restart; everything after that is
  mechanical: ship payload -> publish_native self-proof -> bench5 -> release -> owner's
  finger test.

## 2026-09-26 ~10:35 IST - deploy payload ready, tunnel expired again (owner restart needed)

- **5ecf43b (pushed)**: Main.java pump recovered from the device jar by jadx decompile -
  the lost session's exact changes: ArrayBlockingQueue(1) (SynchronousQueue's offer with no
  waiting poller DROPPED the rc that arrived during readAll, so every operation burned the
  full 30s ceiling), pumpBinder() (a cheap getService("activity") between <=250ms poll
  slices - an idle app_process JVM does not get binder callbacks serviced promptly), one
  nanoTime deadline. Rebuilt through the documented toolchain (ensure-jdk -> JDK 25,
  setup-toolchain -> ecj + d8 8.2.2-dev min-api 31 release): classes.dex strings are
  diff-clean against the field-proven device jar (same D8 header sha-1). module/bin now
  carries the rebuild = source/binary parity; the device jar stays in recovery/ as fallback.
- Suites on the full reconciled tree: an earlier concurrent run showed 83 timing failures -
  caused by my own toolchain builds saturating the sandbox CPU mid-run (all failures were
  bounded-wait assertions); the clean re-run is green through section 47 so far (231/0),
  including the new 7b (refused-no-op-write regression, chmod 444 on the rig's cpu6/online
  = the exact device scenario) and the section 94 argv assertions (input/am dispatch).
- Deploy payload built: /tmp/deploy310.tgz (210KB) = 9 scripts + module.prop v3.10.1/79 +
  rebuilt natives (both ABIs) + jar + bench5.sh. Install plan: place into
  /data/adb/modules/axion_spsm + /data/adb/spsm/scripts, then let the NEW lib's own
  publish_native republish and prove everything (jar via su 2000 rc=2, binaries by loading
  them) - self-testing deploy.
- bench5.sh (tools/remote/bench5.sh, in payload): foreign-slot gestures with the FIXED
  dispatch - recognition alone no longer passes, the focus must actually move; corrected
  recents injection (bench4's moved 79px > the 40px hold tolerance and was correctly read
  as a home swipe - the harness was wrong, not the recognizer): 30px over 500ms; asserts
  dispatch_failures=0 in gesturemon.log; exit race live (deactivate 8s into the grace
  pass) expecting bounded stand-down, NO false "want [1] got [1]" drift, no safety_force,
  zero stranding; clean-cycle scorecard.
- **BLOCKED on tunnel**: Pinggy free tier expired (~60min sessions; the owner's restart was
  ~2h ago). Edge now closes the connection on both ssh and scp. Payload is ready to ship
  the moment the tunnel is back - owner: please restart the Pinggy tunnel in Termux.

## 2026-09-26 ~10:05 IST - reconciliation committed: v3.10.1 tree = device v3.9.2 + both field bugs fixed

- **a87a221 (pushed)**: adopted the device-verified v3.9.2 scripts (three-way diff proved
  v3.9.1 == GitHub HEAD for every script, and v3.9.2 a strict superset for lib.sh - nothing
  was lost by adopting wholesale) + the device binaries (tool jar 12a80b49, any-slot
  gesturemon) + module.prop v3.10.1/79.
- **Fix 1 - false drift (the 70s exit)**: root cause was NOT string comparison. The kernel
  refused cpu6/online in BOTH directions (sleep write never took - the applied journal says
  1, honestly - and the identical write-back got EPERM). The restore counted the refused
  no-op as "failed ... want [1] got [1]" -> 1 drifted knob -> safety valves -> 70s. Now a
  refused write whose value ALREADY stands at the recorded original is logged "wrote".
  An unreadable value still fails (guard compares a real reading). Regression test 7b:
  chmod 444 on the rig's cpu6/online reproduces the exact device scenario end to end.
- **Fix 2 - gesture dispatch (recognised swipes went nowhere)**: gesturemon's defaults were
  the native cmd binary, whose binder dies in the recognizer's nohup'd root context
  ("Failure calling service input: Failed transaction" - 3 recognitions, 0 dispatches in
  bench4). Defaults now the app_process wrappers (input keyevent 3 / am start ...), which
  the engine itself uses from the same context class all day. cfg still overrides; section
  94 now asserts the argv the recognizer is started with.
- **Fix 3 - any-slot recognizer in SOURCE**: HEAD's gesturemon.c still had the slot-0-only
  filter (the panel hands real swipes slots 2/3/5/7). Rewrote the MT state machine: first
  finger DOWN owns the gesture on any slot, other fingers are noise until the owner lifts.
  Zig 0.13 downloaded, both ABIs rebuilt (arm64 58,360B). Device binaries kept in recovery/.
- **Jar source parity recovered**: jadx-decompiled the device jar's classes.dex - the lost
  session's Main.java changes were: ArrayBlockingQueue(1) (a SynchronousQueue offer with no
  waiter DROPPED the rc arriving during readAll -> every op cost the full 30s ceiling),
  pumpBinder() = a cheap getService("activity") between <=250ms poll slices (an idle
  app_process JVM does not get binder callbacks serviced promptly), one nanoTime deadline.
  Ported all three into tool/src/Main.java with the evidence in comments, rebuilt via
  ecj+d8: strings now DIFF-CLEAN against the device jar (same D8 header sha, only binary
  gobblygook differs). module/bin jar = the rebuild (source<->binary parity); the
  field-proven device jar stays in recovery/ as fallback.
- Suites: run 1 on the adopted tree = **742/0 + 252/0 + 19/0 + 22/0 all green**. A second
  run showed 83 failures - all timing-bounded waits, cause = my own toolchain builds
  (pip/jadx/ecj/d8) hammering the sandbox CPU concurrently; re-running clean now (run 3,
  early sections green). Next after suites: deploy v3.10.1 to the phone, bench5 (activate/
  exit/deep-grace/exit-race), synthetic gesture proof with the FIXED dispatch (sendevent
  harness from bench4, corrected recents injection: <=40px movement, 400ms hold), stranding
  scan, then release + owner's finger test.

## 2026-09-26 ~09:25 IST - forensics complete, the lost session's state fully mapped

- **The phone runs v3.9.2/78** - the lost session's own version line, never committed. Its
  scripts are the repo's perf round (deep grace, native cmd paths, spsm-tool, gesturemon)
  PLUS post-commit fixes, all recovered into `recovery/device-2026-09-25/` and pushed (c3b72f6):
  1. **spsm-tool rc=134 root cause + fix**: KernelSU keeps /data/adb at 700 root, so uid 2000
     cannot read the jar and ART aborts with a misleading DEX2OAT message. Fix: runtime copy at
     /data/local/tmp/spsm/spsm-tool.jar, proof-of-life runs from THAT copy. Device jar
     (6,256 B, md5 12a80b49) differs from the committed one (5,856 B, f2ef2c46) - strings diff
     in progress; repair.log proves the device jar works (171 pkgs read, 111 writes, 0 failures).
  2. **The 21:27 stranding contained**: journal-first ordering (records land BEFORE writes,
     batch and fan), "applying" journal state so the exit sweep never deletes an in-flight
     knob's records, bounded exit wait on deep_restrict.pid, workers stand down when $ACTIVE
     dies, failed reads restore from record instead of being skipped. repair.log: the 31
     buckets + 80 appops stranded that night were repaired (writes=111, rc=0).
  3. **bench4.log (23:48) - the lost session's last benchmark**: deep grace fired at +74s
     exactly as designed; the exit race was caught live (deactivate 8s into a restrict pass ->
     bounded wait -> clean revert); gesturemon RECOGNISED every synthetic swipe
     (home dy=209/177ms, dy=281/210ms, dy=183/129ms) but **every dispatch failed**:
     "cmd: Failure calling service input: Failed transaction".
- **Bug A (gestures)**: the dispatch defaults are `cmd input keyevent 3` / `cmd activity start`
  - the native `cmd` binder fails from gesturemon's nohup'd root context, while the engine's
  app_process `input`/`am` calls work from the same context all day. start_gesturemon passes
  commands explicitly, so the fix is script-side defaults (no binary rebuild; zig is gone from
  the sandbox anyway). C defaults get patched for future builds.
- **Bug B (false drift)**: "WARN cores_sleep did not return: want [1] got [1] " - trailing
  whitespace in the compared reading; one exit cost 70s over it (safety valves + re-verify).
  Fix: trim in the drift comparison. Also silence safety_force writing cpu0/online (EPERM noise;
  cpu0 is not offlineable on this kernel).
- Plan next: adopt the device scripts as the tree of record, apply fixes A+B, suite, redeploy
  as v3.10.1/79, re-benchmark (activate/exit/deep/grace), synthetic-swipe gesture proof with
  working dispatch, then the owner's finger. Release + notes after.

## 2026-09-26 ~08:50 IST - session start, situation assessment

- Sandbox was rewound overnight: `.git/config`, `sshpass`, `+x` bits all had to be rebuilt.
  Recovered the branch from GitHub (`git reset --hard origin/Rewrite` -> `3ed7f10`).
- GitHub state: Rewrite @ `97669b2` after I published v3.10.0 (bump + notes + zip + release)
  earlier today. Releases: v3.9.0 (Sep 25) and v3.10.0 (Sep 26). **No v3.9.1/v3.9.2 tag or
  release exists on GitHub.**
- Device probe (tunnel revived by owner, endpoint libiy-...:42199):
  - `/data/adb/spsm/native/` holds spsm-gesturemon (58,200 B), spsm-screenmon (29,592 B),
    spsm-tool.jar (6,256 B), all dated **Sep 25 23:46** - sizes DIFFER from the repo's
    committed binaries (58,104 / - / 5,856): the lost session rebuilt and deployed past its
    last commit. DEVICE IS THE ONLY COPY - recovering it into the repo this hour.
  - `spsm.log` tail shows a 23:53 exit on NEWER code than the repo: `WARN cores_sleep did not
    return: ... want [1] got [1]` (a comparison bug - warns when values MATCH), then
    `exit: 1 value(s) could not be restored - forcing the safety valves`,
    `safety_force: cores online, governor schedutil`, `revert finished in 18s with 1 drifted
    knob(s)`. The drift was false (want==got) - to fix after recovery.
  - Termux reinstalled (F-Droid -> Play): **new uid 10253**; deviceidle AND netpolicy
    whitelists already carry 10253 (fixed by the lost session or the owner). Battery 49%,
    charging.
- Plan: (1) pull every device-only artifact (scripts, binaries, module.prop, config, logs,
  /data/local/tmp benchmark material) into the repo and push; (2) diff device vs HEAD, adopt
  the newer work as the tree of record; (3) fix the cores_sleep WARN false-drift; (4) suites;
  (5) on-phone benchmark round + gesture proof (input-swipe driven, then owner's finger);
  (6) release whatever wins. Worklog pushed at every step.
